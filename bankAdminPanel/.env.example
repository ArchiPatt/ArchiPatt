# URL сервиса monitoring (RUM → POST /public/ingest). Пусто – телеметрия с клиента отключена.
VITE_MONITORING_URL=http://localhost:4010
# Подпись событий в дашборде (колонка «Источник»)
VITE_RUM_SOURCE=web-bankAdminPanel
